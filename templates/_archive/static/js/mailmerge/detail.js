/**
 * mailmerge/detail.js
 * Alpine.js component for the mail merge job detail / record preview page.
 * (Archived copy)
 */

(function () {
    // Archived copy — original behavior preserved for reference.
})();
